name: Release

on:
  push:
    tags: [ "v*" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            name: windows-x86_64
          - os: macos-latest
            target: aarch64-apple-darwin
            name: macos-aarch64
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            name: linux-x86_64
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            name: linux-aarch64

    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v5

    - name: Install Linux dependencies
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

    - name: Build
      run: cargo build --verbose --release --target ${{ matrix.target }}

    - name: Run tests
      run: cargo test --verbose --release --target ${{ matrix.target }}

    - name: Package
      shell: bash
      run: |
        # 打包可执行文件, 按目标平台命名
        mkdir -p dist
        if [ "${{ runner.os }}" = "Windows" ]; then
          cp target/${{ matrix.target }}/release/openagents.exe dist/
        else
          cp target/${{ matrix.target }}/release/openagents dist/
        fi
        cd dist && 7z a ../openagents-${{ matrix.name }}.zip *

    - name: Upload release assets
      uses: softprops/action-gh-release@v3
      with:
        files: openagents-${{ matrix.name }}.zip
