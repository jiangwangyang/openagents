# AGENTS.md

## 禁止

1. 禁止安装任何软件或依赖，只能向我提出安装申请由我安装。
2. 禁止修改非项目文件，只允许修改当前目录下的文件。
3. 禁止运行程序，代码编写完后仅允许编译检验。

## 工作流

1. 了解阶段：使用`tree -L 4`命令查看当前项目结构。
2. 需求阶段：明确需求，包括输入输出等。若需求模糊，先向我提出澄清问题
3. 方案阶段：规划改动范围 ，包括文件，函数，框架等。待我确认后再生成代码。
4. 生成阶段：按照要求严格生成代码，禁止修改其它内容。
5. 检查阶段：生成后，自我审查是否遵循了上述所有规范，然后汇总归纳。

## 编码规范

1. 默认使用函数编程，禁止编写类和方法，实体类 (绑定数据)除外。
2. 函数的参数，返回值需要明确声明类型。
3. 业务代码需在一个函数内编写完，禁止拆分为多个函数调用。
4. (Python) 声明类型时使用内置类型，禁止使用`typing`模块中的泛型类。
5. (Python) 统一使用`#`注释，禁止使用'''或者"""注释。
